interface NotificationDropdownProps {
  isMobile?: boolean
  disabled?: boolean
}

export function NotificationDropdown({ isMobile = false, disabled = false }: NotificationDropdownProps) {
  // Return null since the notification system has been removed
  return null
}
